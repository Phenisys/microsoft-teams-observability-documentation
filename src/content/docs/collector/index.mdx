---
title: Collector
description: User documentation for the ms-teams-agent Standalone and Dynatrace Extension deployment models, with v1 deprecated.
sidebar:
  label: Overview
  order: 0
---

import { LinkCard, CardGrid, Aside } from '@astrojs/starlight/components';

The **MS Teams Observability Collector** (`ms-teams-agent`) retrieves Microsoft Teams telemetry and sends it to configured backends.

The collector is available in two active deployment variants: **Standalone (current)** and a **Dynatrace Extension** that runs inside an ActiveGate. **v1 is deprecated**.

<Aside type="caution">
Use Standalone for all new deployments. If you are still on v1, plan migration with <a href={`${import.meta.env.BASE_URL}collector/standalone/migration/`}>Migrate from v1</a>.
</Aside>

## Standalone

<CardGrid>
  <LinkCard
    title="Overview"
    description="Main Standalone documentation entry point."
    href={`${import.meta.env.BASE_URL}collector/standalone/`}
  />
  <LinkCard
    title="Installation"
    description="Install and start the Standalone collector."
    href={`${import.meta.env.BASE_URL}collector/standalone/installation/`}
  />
  <LinkCard
    title="Configuration"
    description="Set authentication, outputs, and feature flags for Standalone."
    href={`${import.meta.env.BASE_URL}collector/standalone/configuration/`}
  />
  <LinkCard
    title="CLI"
    description="Run, validate, service, and state commands."
    href={`${import.meta.env.BASE_URL}collector/standalone/cli/`}
  />
  <LinkCard
    title="Service Management"
    description="Run Standalone as a Linux systemd service."
    href={`${import.meta.env.BASE_URL}collector/standalone/service/`}
  />
</CardGrid>

## Extension

<CardGrid>
  <LinkCard
    title="Dynatrace Extension"
    description="Run the collector inside a Dynatrace ActiveGate — managed lifecycle, no YAML file required."
    href={`${import.meta.env.BASE_URL}collector/extension/`}
  />
</CardGrid>

## v1 (Deprecated)

Legacy v1 pages are archived for existing installations and should not be used for new deployments. See <a href={`${import.meta.env.BASE_URL}reference/archive/v1-deprecated/`}>v1 Deprecated Archive</a>.
