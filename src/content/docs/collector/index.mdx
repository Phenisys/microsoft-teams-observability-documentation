---
title: Collector
description: User documentation for the ms-teams-agent Standalone and Dynatrace Extension deployment models, with v1 deprecated.
sidebar:
  label: Overview
  order: 0
---

import { LinkCard, CardGrid, Aside } from '@astrojs/starlight/components';

The **MS Teams Observability Collector** (`ms-teams-agent`) retrieves Microsoft Teams telemetry and sends it to configured backends.

The collector is available in two active deployment variants: **Standalone (current)** and a **Dynatrace Extension** that runs inside an ActiveGate. **v1 is deprecated**.

<Aside type="caution">
Use Standalone for all new deployments. If you are still on v1, plan migration with <a href={`${import.meta.env.BASE_URL}collector/v2/migration/`}>Migrate from v1</a>.
</Aside>

## Standalone

<CardGrid>
  <LinkCard
    title="Azure Permissions"
    description="Microsoft Graph app registration and required permissions."
    href={`${import.meta.env.BASE_URL}collector/azure-permissions/`}
  />
  <LinkCard
    title="Installation"
    description="Install, validate, and run the collector."
    href={`${import.meta.env.BASE_URL}collector/v2/installation/`}
  />
  <LinkCard
    title="Configuration"
    description="Set authentication, outputs, and feature flags for Standalone."
    href={`${import.meta.env.BASE_URL}collector/v2/configuration/`}
  />
  <LinkCard
    title="CLI"
    description="Command reference for run, validate, service, and state."
    href={`${import.meta.env.BASE_URL}collector/v2/cli/`}
  />
  <LinkCard
    title="Service"
    description="Install and manage the collector service."
    href={`${import.meta.env.BASE_URL}collector/v2/service/`}
  />
  <LinkCard
    title="Upgrade"
    description="Routine patch or minor update procedure."
    href={`${import.meta.env.BASE_URL}collector/v2/upgrade/`}
  />
  <LinkCard
    title="Migrate from v1"
    description="Migrate from legacy v1 CLI and configuration to Standalone."
    href={`${import.meta.env.BASE_URL}collector/v2/migration/`}
  />
</CardGrid>

## Extension

<CardGrid>
  <LinkCard
    title="Dynatrace Extension"
    description="Run the collector inside a Dynatrace ActiveGate — managed lifecycle, no YAML file required."
    href={`${import.meta.env.BASE_URL}collector/extension/`}
  />
</CardGrid>

## v1 (Deprecated)

Legacy v1 pages are archived for existing installations and should not be used for new deployments. See <a href={`${import.meta.env.BASE_URL}reference/archive/v1-deprecated/`}>v1 Deprecated Archive</a>.
