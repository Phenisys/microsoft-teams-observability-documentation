---
title: Application — Overview
description: User guide for the MS Teams Observability Grafana app — 6 tabs covering call quality, site analysis, user activity, and status.
sidebar:
  label: Overview
  order: 0
---

import { LinkCard, CardGrid, Aside } from '@astrojs/starlight/components';

The MS Teams Observability app is installed in your Grafana environment and provides operational visibility into your Microsoft Teams estate. It queries Loki logs exported by the collector.

**Version:** 1.2.0 | **Developed by:** Phenisys | **Platform:** Grafana Plugin

## Navigation

The app has **6 tabs** in the top navigation bar:

| Tab | Route | Description |
|-----|-------|-------------|
| **Home** | `/home` | Cockpit overview: KPIs, call health, platform distribution |
| **Sites** | `/sites` | Quality by location: geomap, NPA compliance, analytics |
| **Calls** | `/calls` | Filterable call list — click a call to drill into Call Overview |
| **Users** | `/users` | Per-user analytics: calls, devices, network, recent calls |
| **Issues** | `/issues` | Microsoft 365 service health incidents |
| **Status** | `/configuration` | License, feature status, outgoing connection tests |

<Aside type="note">
The **Call Overview** is not a separate tab — it opens as a drilldown when you click a Call ID on the Calls or Users pages. The breadcrumb navigation returns you to the list.
</Aside>

<CardGrid>
  <LinkCard
    title="Installation"
    description="Install the plugin from the Grafana Plugin Catalog or via ZIP upload."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/installation/`}
  />
  <LinkCard
    title="Home"
    description="KPI cards (call count, healthy-calls %, caller count) and quality charts."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/home/`}
  />
  <LinkCard
    title="Sites"
    description="Geomap, NPA compliance, analytics charts, and site inventory."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/sites/`}
  />
  <LinkCard
    title="Calls"
    description="Key/op/value filter bar, sortable table, drilldown to Call Overview."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/calls/`}
  />
  <LinkCard
    title="Users"
    description="Per-user table: identity, network, call stats, devices, recent calls."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/users/`}
  />
  <LinkCard
    title="Issues"
    description="Active and resolved Microsoft 365 service health incidents."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/issues/`}
  />
  <LinkCard
    title="Status"
    description="License panel, feature status table, and outgoing connection tests."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/status/`}
  />
  <LinkCard
    title="Provisioning"
    description="Grafana AppConfig: Microsoft Graph, datasource, GeoIP toggle, subnet mappings."
    href={`${import.meta.env.BASE_URL}backends/grafana/app/provisioning/`}
  />
</CardGrid>

---

## Health Indicators

| Status | Visual | Meaning |
|--------|--------|---------|
| Good | Green | Call met quality thresholds |
| Poor | Red | Call had quality degradation |
| Unclassified | Grey | No quality data available |

## Demo vs Live Mode

The app supports two modes via the datasource configuration:

- **Demo Mode** — uses built-in sample data (JSON fixtures in `src/data/`). No collector or Loki required.
- **Live Mode** — queries a real Loki datasource with data from the collector. Requires a valid license.

Toggle between modes in the provisioning page: **Administration → Plugins → MS Teams Observability → Configuration**.

<Aside type="note">
Live Mode requires a valid Phenisys license. See [License](../../../getting-started/license/) for how to obtain it.
</Aside>
