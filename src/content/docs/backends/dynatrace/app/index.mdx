---
title: Explore the Dynatrace Application
description: Functional user guide for the MS Teams Observability Dynatrace app.
sidebar:
  label: Overview
  order: 0
---

import { LinkCard, CardGrid, Aside } from '@astrojs/starlight/components';
import dynatraceAppHomePage from '../../../../../assets/images/dynatrace-app-home-page.png';

MS Teams Observability is a Dynatrace application that brings Microsoft Teams call quality data into your Dynatrace environment. It allows you to monitor call health, diagnose quality issues, track user activity, inspect network performance by site, and follow Microsoft service incidents from one interface.

**Repository:** <a href="https://github.com/Phenisys/microsoft-teams-observability-dynatrace-app" target="_blank" rel="noopener noreferrer">microsoft-teams-observability-dynatrace-app</a> | **Latest release:** <a href="https://github.com/Phenisys/microsoft-teams-observability-dynatrace-app/releases/latest" target="_blank" rel="noopener noreferrer">GitHub Releases</a>

## Navigation

<CardGrid>
  <LinkCard
    title="Home"
    description="7-day summary of call activity and quality."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/home/`}
  />
  <LinkCard
    title="Sites"
    description="Call quality and network performance by geographic site."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/sites/`}
  />
  <LinkCard
    title="Calls"
    description="Searchable and filterable list of all calls."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/calls/`}
  />
  <LinkCard
    title="Call Overview"
    description="Detailed analysis of one call, including AI analysis."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/call-overview/`}
  />
  <LinkCard
    title="Users"
    description="Per-user activity, quality stats, and device/network details."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/users/`}
  />
  <LinkCard
    title="Issues"
    description="Active and resolved Microsoft service health incidents."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/issues/`}
  />
  <LinkCard
    title="Configuration"
    description="All app settings: license, integrations, pipeline, domains, permissions."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/configuration/`}
  />
  <LinkCard
    title="Installation"
    description="Install the app and activate Live mode."
    href={`${import.meta.env.BASE_URL}backends/dynatrace/app/installation/`}
  />
</CardGrid>

## Screenshot

<figure>
  <img src={dynatraceAppHomePage.src} alt="MS Teams Observability app Home page" />
  <figcaption>Application entry page (Home).</figcaption>
</figure>

## Common UI Patterns

### Timeframe Selector

Available on **Sites**, **Calls**, **Users**, and **Issues** pages.

| Label | Range |
|-------|-------|
| Last 15 minutes | `now()-15m` to `now()` |
| Last 30 minutes | `now()-30m` to `now()` |
| Last 1 hour | `now()-1h` to `now()` |
| Last 2 hours | `now()-2h` to `now()` |
| Last 6 hours | `now()-6h` to `now()` |
| Last 24 hours | `now()-24h` to `now()` |
| Last 3 days | `now()-3d` to `now()` |
| Last 7 days | `now()-7d` to `now()` |
| Last 30 days | `now()-30d` to `now()` |

Custom date/time selection is also available. Selected values are stored in URL (`?from=...&to=...`).

### Update Button

Present on pages with filters. It applies the current filter/timeframe values.

- Highlighted when values were changed but not applied.
- Disabled during query execution.
- Disabled when filter syntax is invalid.

### Advanced Filter Field

Used on **Calls**, **Users**, and **Sites** pages.

- Structured syntax with suggestions while typing.
- General format: `key operator "value"`.
- Example: `health = "Good"`.
- Stored in URL (`?filter=...`).

### Open with Notebooks

Available in sections that expose a Dynatrace query. Opens the underlying query in Dynatrace Notebooks with current context.

### Health Indicators

| Status | Visual | Meaning |
|--------|--------|---------|
| Good | Green icon | Call met quality thresholds |
| Poor | Red icon | Call had quality degradation |
| Unclassified | Grey icon | No quality data available |

## Toast Notifications

- **Success** (green): action completed.
- **Warning** (yellow): action completed with caveats.
- **Critical** (red): action failed or configuration invalid.
- **Info** (blue): operation in progress.

## Demo Mode

Demo Mode replaces live data with mock data. It can be enabled/disabled from <a href={`${import.meta.env.BASE_URL}backends/dynatrace/app/configuration/`}>Configuration</a>.

<Aside type="note">
When Demo Mode is active, indicators appear in the License panel and some advanced features are restricted.
</Aside>
