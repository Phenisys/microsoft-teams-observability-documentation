---
title: Configure the Dynatrace Application
description: "Functional reference of all configuration panels and their effects in the app."
sidebar:
  label: Configuration
  order: 7
---

import { Aside } from '@astrojs/starlight/components';
import dynatraceAppConfigurationPage from '../../../../../assets/images/dynatrace-app-configuration-page.png';
import dynatraceAppConfigurationPageAiPanel from '../../../../../assets/images/dynatrace-app-configuration-page-ai-panel.png';
import dynatraceAppConfigurationPageBucketConfigurationPanel from '../../../../../assets/images/dynatrace-app-configuration-page-bucket_configuration-panel.png';
import dynatraceAppConfigurationPageDemoPanel from '../../../../../assets/images/dynatrace-app-configuration-page-demo-panel.png';
import dynatraceAppConfigurationPageDomainsFilterPanel from '../../../../../assets/images/dynatrace-app-configuration-page-domains_filter-panel.png';
import dynatraceAppConfigurationPageFeatureStatusPanel from '../../../../../assets/images/dynatrace-app-configuration-page-feature_status-panel.png';
import dynatraceAppConfigurationPageIpamPanel from '../../../../../assets/images/dynatrace-app-configuration-page-ipam-panel.png';
import dynatraceAppConfigurationPageLicensePanel from '../../../../../assets/images/dynatrace-app-configuration-page-license-panel.png';
import dynatraceAppConfigurationPageMsConfigurationPanel from '../../../../../assets/images/dynatrace-app-configuration-page-ms_configuration-panel.png';
import dynatraceAppConfigurationPageOpenpipelinePanel from '../../../../../assets/images/dynatrace-app-configuration-page-openpipeline-panel.png';
import dynatraceAppConfigurationPageOutgoingConnectionsPanel from '../../../../../assets/images/dynatrace-app-configuration-page-outgoing_connections-panel.png';
import dynatraceAppConfigurationPageSiteFileManagementPanel from '../../../../../assets/images/dynatrace-app-configuration-page-site_file_management-panel.png';
import dynatraceAppConfigurationPageUserPermissionPanel from '../../../../../assets/images/dynatrace-app-configuration-page-user_permission-panel.png';

> **Path:** `/configuration`

## What This Page Controls

The Configuration page is the central settings hub for the app. It controls license state, connectivity, ingestion pipeline, data scoping, and advanced features.

Breadcrumb navigation: `Home > Configuration`

## Screenshot

<figure>
  <img src={dynatraceAppConfigurationPage.src} alt="Configuration page" />
  <figcaption>Configuration page overview.</figcaption>
</figure>

<Aside type="caution">
Panels with pending modifications show an "Unsaved changes" chip. Leaving the page can trigger a browser warning.
</Aside>

## Panel Layout

| Row | Left panel | Right panel |
|-----|-----------|-------------|
| 1 | [License](#1-license) | [Demo Mode](#2-demo-mode) |
| 2 | [Feature Status](#3-feature-status) | — |
| 3 | [Outgoing Connections](#4-outgoing-connections) | — |
| 4 | [Bucket Configuration](#5-bucket-configuration) | [OpenPipeline](#6-openpipeline) |
| 5 | [Site File Management](#7-site-file-management) | — |
| 6 | [IPAM](#8-ipam) | [AI Analysis](#9-ai-analysis) |
| 7 | [Microsoft Configuration](#10-microsoft-configuration) | [Domains Filter](#11-domains-filter) |
| 8 | [Permissions](#12-permissions) | — |

## 1. License

<figure>
  <img src={dynatraceAppConfigurationPageLicensePanel.src} alt="License panel" />
  <figcaption>License panel.</figcaption>
</figure>

This panel shows license validity, expiration, and tenant usage information.

Displayed fields:

| Field | Description |
|-------|-------------|
| Start date | License start date |
| End Date | Expiration date |
| Days Left | Remaining days |
| Status chip | Trial / Active / Inactive |
| Active Users meter | Current usage vs allowed |
| Microsoft Tenant ID | Tenant ID (masked by default) |

Actions:

| Action | Description |
|--------|-------------|
| Eye icon | Show/hide tenant ID |
| Copy | Copy tenant ID |

## 2. Demo Mode

<figure>
  <img src={dynatraceAppConfigurationPageDemoPanel.src} alt="Demo mode panel" />
  <figcaption>Demo mode panel.</figcaption>
</figure>

This panel switches the app between demo data and live operational data.

| Button | Behavior |
|--------|----------|
| Enable | Enables Demo Mode after confirmation |
| Disable | Disables Demo Mode after confirmation |

When active, Demo Mode restricts custom timeframe selection, call export, and AI analysis.

## 3. Feature Status

<figure>
  <img src={dynatraceAppConfigurationPageFeatureStatusPanel.src} alt="Feature status panel" />
  <figcaption>Feature status panel.</figcaption>
</figure>

This panel displays feature-level enablement and ingestion health over the last 7 days.

| Column | Description |
|--------|-------------|
| Feature | Data collection feature |
| Status | Enabled / Disabled |
| Health | No errors / X errors |

Actions:

- Right-click Health cell -> open details in Dynatrace app.

## 4. Outgoing Connections

<figure>
  <img src={dynatraceAppConfigurationPageOutgoingConnectionsPanel.src} alt="Outgoing connections panel" />
  <figcaption>Outgoing connections panel.</figcaption>
</figure>

This panel verifies network/API reachability for all external dependencies used by the app.

| Column | Description |
|--------|-------------|
| Status | Reachability indicator |
| Code | HTTP status code |
| Service | Service name |
| Endpoint | Target URL |
| Message | Last ping result |
| Latency | Round-trip latency |

Actions:

- Ping one endpoint.
- Ping all endpoints.

## 5. Bucket Configuration

<figure>
  <img src={dynatraceAppConfigurationPageBucketConfigurationPanel.src} alt="Bucket configuration panel" />
  <figcaption>Bucket configuration panel.</figcaption>
</figure>

This panel manages the storage bucket used for MS Teams telemetry data.

States:

| State | Behavior |
|-------|----------|
| Loading | Spinner shown |
| Bucket exists | Bucket details shown |
| Bucket missing | Creation form shown |
| Error | Error container with actions |

Creation fields:

| Field | Required | Description |
|-------|----------|-------------|
| Display Name | No | Human-readable bucket name |
| Retention Days | Yes | Log retention period |

## 6. OpenPipeline

<figure>
  <img src={dynatraceAppConfigurationPageOpenpipelinePanel.src} alt="OpenPipeline panel" />
  <figcaption>OpenPipeline panel.</figcaption>
</figure>

This panel stores and validates credentials used to configure the OpenPipeline ingestion path.

| Field | Required | Description |
|-------|----------|-------------|
| Access Token | Yes | Dynatrace API token with `settings.read` and `settings.write` |

Validation statuses:

| Status | Message |
|--------|---------|
| Green | Open Pipeline Configuration is valid |
| Yellow | Configuration is being validated |
| Red | No valid Open Pipeline Configuration detected |

Buttons:

- Save
- Validate
- Generate Configuration
- Reset

## 7. Site File Management

<figure>
  <img src={dynatraceAppConfigurationPageSiteFileManagementPanel.src} alt="Site file management panel" />
  <figcaption>Site file management panel.</figcaption>
</figure>

This panel uploads and maintains the subnet-to-location CSV consumed by the [Sites](/backends/dynatrace/app/sites/) page.

### File status

| Indicator | Meaning |
|-----------|---------|
| Sites file exists | Mapping file present |
| No sites file | No mapping file present |
| Size / Modified / By | File metadata |
| X sites loaded | Parsed location entries |

### Actions

| Button | Description |
|--------|-------------|
| Choose CSV file | Select file |
| Upload/Replace | Upload validated CSV |
| Download | Download current `sites.csv` |
| Delete | Remove current file |

### Required CSV columns

| Column | Description |
|--------|-------------|
| `subnet` | IPv4 CIDR |
| `location` | Site name |
| `country` | Country code |
| `latitude` | Latitude |
| `longitude` | Longitude |

Validation errors disable upload and are displayed inline.

## 8. IPAM

<figure>
  <img src={dynatraceAppConfigurationPageIpamPanel.src} alt="IPAM panel" />
  <figcaption>IPAM panel.</figcaption>
</figure>

This panel defines the URL template used by IP lookup context-menu actions.

| Field | Required | Description |
|-------|----------|-------------|
| Link Template | Yes | URL containing `{{ip}}` placeholder |

Example: `https://ipam.company.com/ip/{{ip}}/details`

Buttons:

- Save
- Test Link
- Reset

Effect: enables Lookup IPAM context-menu action in Sites and Call Overview tables.

## 9. AI Analysis

<figure>
  <img src={dynatraceAppConfigurationPageAiPanel.src} alt="AI analysis panel" />
  <figcaption>AI Analysis panel.</figcaption>
</figure>

This panel configures the AI provider, credential, and prompt used by [Call Overview](/backends/dynatrace/app/call-overview/).

| Field | Required | Description |
|-------|----------|-------------|
| Tool | Yes | Selected AI model |
| Token | Yes | Credential from vault |
| Prompt | Yes | System prompt |

Buttons:

- Save
- Test
- Reset

Validation statuses are shown (valid / testing / invalid).

## 10. Microsoft Configuration

<figure>
  <img src={dynatraceAppConfigurationPageMsConfigurationPanel.src} alt="Microsoft configuration panel" />
  <figcaption>Microsoft configuration panel.</figcaption>
</figure>

This panel configures Microsoft Graph credentials used by live call-detail features.

| Field | Required | Description |
|-------|----------|-------------|
| Credential Name | Yes | Vault credential |
| Client ID | No | Azure app client ID |
| Tenant ID | Yes | Azure tenant ID |
| Grant Type | Yes | Client Credentials or Demo |
| Scope | Yes | OAuth scope |

Buttons:

- Save
- Test
- Reset

Required for Call Overview download and AI actions.

## 11. Domains Filter

<figure>
  <img src={dynatraceAppConfigurationPageDomainsFilterPanel.src} alt="Domains filter panel" />
  <figcaption>Domains Filter panel.</figcaption>
</figure>

This panel defines which email domains are included in site analytics data scope.

Format examples:

- `@example.com`
- `@sub.example.com`

Actions:

- Add domain
- Remove domain
- Save list
- Reset list

Validation catches invalid format and duplicates.

Effect: impacts only [Sites](/backends/dynatrace/app/sites/) data scope.

## 12. Permissions

<figure>
  <img src={dynatraceAppConfigurationPageUserPermissionPanel.src} alt="Permissions panel" />
  <figcaption>Permissions panel.</figcaption>
</figure>

This panel manages access rights and role-based permissions for the application.

## Common Behaviors

### Unsaved changes

- Unsaved changes chip appears when values differ from saved state.
- Browser warning appears on page leave if unsaved changes exist.

### Toast notifications

| Operation | Success | Failure |
|-----------|---------|---------|
| Save | Success toast | Critical toast |
| Reset | Success toast | Critical toast |
| Test | Success toast | Critical toast with details |

### Auto-validation on load

Panels with saved credentials are auto-tested on load:

- Microsoft Configuration
- AI Analysis
- OpenPipeline

<Aside type="note">
If multiple panels fail at the same time, verify credentials, token validity, and external connectivity first.
</Aside>
